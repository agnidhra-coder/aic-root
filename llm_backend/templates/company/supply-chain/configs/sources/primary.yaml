# Template source binding for a supply-chain tenant: weekly, keyed by supplier.
# `path` is where `attach_source_data` puts an uploaded CSV; the company is
# `awaiting_data` until that file exists.
source_id: scm_weekly
type: csv
path: data/raw/primary.csv
date_column: Week Start
entity_columns: [Region, Product category, Supplier]
refresh_cadence: weekly
as_of: null
