PORT=3001
CORS_ORIGIN=http://localhost:3000

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=your-secret-key

JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# The Python KPI-analysis engine. Server-to-server only — it has no
# authentication and binds 127.0.0.1, so it must never be reachable from a
# browser. Defaults to http://127.0.0.1:8000 when unset.
PYTHON_API_URL=http://127.0.0.1:8000
